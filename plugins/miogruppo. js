let handler = async (m, { conn }) => {
  // 🔁 metti qui il TUO link
  const link = 'https://chat.whatsapp.com/IGn4PkanPDn6sjG0J2yubV?mode=gi_t'

  const testo =
    `🔥 *Entra nel mio gruppo ufficiale!*\n` +
    `${link}`

  await conn.sendMessage(m.chat, { text: testo }, { quoted: m })
}

handler.help = ['miogruppo']
handler.tags = ['info']
handler.command = /^miogruppo$/i

export default handler
