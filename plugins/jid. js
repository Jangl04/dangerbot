const handler = async (m, { conn }) => {
  const jid = conn.decodeJid(m.sender);
  return m.reply(`🆔 Il tuo JID è:\n${jid}`);
};

handler.help = ['jid'];
handler.tags = ['tools'];
handler.command = /^jid$/i;

export default handler;
